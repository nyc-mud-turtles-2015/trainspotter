<ul class = "content" >
<% if logged_in? %>
  <% collections.each do |c| %>
    <% if c.visible_to_user?(current_user) %>
      <li><h2><%= link_to c.title, collection_path(c) %></h2></li>
        <% if belongs_to_current_user?(c) %>
          <%= button_to "Delete", collection_path(c), :method => :delete %>
        <% end %>
    <% end %>
  <% end %>
<% else %>
  <% collections.each do |c| %>
    <% if c.public? %>
      <li><h2><%= link_to c.title, collection_path(c) %></h2></li>
    <% end %>
  <% end %>
<% end %>
</ul>


