<ul class = "content" >

<% @observations.each do |o| %>
  <li><%= o.description %> </li>
  <ul>
    <%= link_to (image_tag o.image.url(:thumb)), collection_observation(o.id) %>
  </ul>
<% end %>

</ul>

<%= button_to "Add Observations", new_collection_observation, method: "get" %>
