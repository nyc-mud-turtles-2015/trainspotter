<ul class = "content" >

<% @observations.each do |o| %>
  <li><%= o.description %> </li>
  <ul>
    <%= link_to (image_tag o.image.url), observation_path(o) %>
  </ul>
<% end %>

</ul>

<%= button_to "Add Observations", new_observation_path, method: "get" %>
